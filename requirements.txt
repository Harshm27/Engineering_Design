cadquery==2.8.0
numpy>=1.24
pillow>=10.0
# web front end (the `serve` subcommand); not needed for the CLI or self-test
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
# the AI reading step (upload a drawing); needs ANTHROPIC_API_KEY
anthropic>=0.40
